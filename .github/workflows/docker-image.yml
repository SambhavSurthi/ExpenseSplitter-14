name: Docker Fullstack CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push backend image
        uses: docker/build-push-action@v5
        with:
          context: ./Backend/expense-splitter #💀 Change to the backend project path in git repo
          file: ./Backend/expense-splitter/backend.Dockerfile #💀 Change to the backend.Dockerfile path in git repo
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/expense-splitter-backend-14:latest #💀 change to backend image name you want

      - name: Build and push frontend image
        uses: docker/build-push-action@v5
        with:
          context: ./Frontend/expense-splitter #💀 Change to the frontend project path in git repo
          file: ./Frontend/expense-splitter/frontend.Dockerfile #💀 Change to the frontend.Dockerfile path in git repo
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/expense-splitter-frontend-14:latest #💀 change to frontend image name you want
